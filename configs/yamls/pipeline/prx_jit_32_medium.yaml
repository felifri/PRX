# @package _global_

# PRX Medium (~3.8B) + T5-Gemma 2B + Identity VAE (pixel-space, no compression)
# For dose-response scaling experiment isolating parameter count

defaults:
  - prx_standard
  - override /model@diffusion_model: prx_jit_32_medium
  - override /vae@diffusion_vae: identity
  - override /scheduler@diffusion_scheduler: euler_x_prediction_shifted
