# @package _global_
defaults:
  - Flux-Baseline
  - _self_

name: iREPA-Dinov3

algorithms:
  irepa:
    _target_: prx.algorithm.repa.iREPA
    lambda_weight: 0.5
    layer_index: 7
    encoder: "dinov3_vitl16"
    compile_encoder: true
